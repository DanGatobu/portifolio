// // // Adjust based on your image URL and element selector
// const image = document.querySelector('.nvm');
// const imageRatio = image.naturalWidth / image.naturalHeight;
// image.style.height = `${image.offsetWidth * imageRatio}px`;
